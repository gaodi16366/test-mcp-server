# Auth0 tenant domain (no https://)
AUTH0_DOMAIN=your-tenant.jp.auth0.com

# This server's public URL — must match the Auth0 API identifier (see README)
SERVER_URL=https://YOUR_NGROK_URL

# Port to listen on
PORT=3000
